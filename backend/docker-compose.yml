services:

  postgres:
    restart: always
    container_name: postgres
    image: postgres:15
    volumes:
      - postgres_music_volume:/var/lib/postgresql/data/
    env_file:
      - .env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -V" ]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - ${DB_PORT}:${DB_PORT}
    command: -p ${DB_PORT}

  backend:
    restart: always
    container_name: backend
    build:
      context: .
      dockerfile: "Dockerfile"
    env_file:
      - .env
    ports:
      - 8000:8000
    depends_on:
      - postgres
    volumes:
      - ./src:/src

volumes:
  postgres_music_volume:


